export default interface ReportStatementListSpec {
  year: number;
  initialBalance: number;
  contributionSummary: number;
  annualInterest: number;
  yields: number;
  withdrawalsSummary: number;
  refund: number;
  netTotal: number;
}